
//////////////////////////////////////////////////////////////////////////////////////////////////////////////

//const sdds:KeyboardEvent
//document.body.addEventListener("keydown", (event:KeyboardEvent) => {});

// Binding<KeyPress, Move>()
// Binding<KeyPress, Dig>()

interface IBinding
	{
	}

interface IKeyPressBinding<T extends object> extends IBinding
	{
	}

type KeyPress = () => void;

let sd:IKeyPressBinding<KeyPress>;

//////////////////////////////////////////////////////////////////////////////////////////////////////////////

class GamePad
	{
	public constructor(private target:EventTarget)
		{
		target.addEventListener("keypress", ()=>{});
		}

	public ddf():void
		{
		//this.target.removeEventListener
		}
	}

//////////////////////////////////////////////////////////////////////////////////////////////////////////////

//new GamePad(window);

//////////////////////////////////////////////////////////////////////////////////////////////////////////////

export
	{
	GamePad
	};
